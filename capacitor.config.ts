import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // TODO: zamień na docelowy, unikalny identyfikator (reverse-DNS) przed publikacją w sklepach
  appId: 'com.example.mentalwellness',
  appName: 'Mental Wellness',
  webDir: 'dist',
  backgroundColor: '#0b1120',
};

export default config;
